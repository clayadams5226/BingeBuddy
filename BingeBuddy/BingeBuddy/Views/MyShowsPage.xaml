<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:BingeBuddy.ViewModels"
             xmlns:models="clr-namespace:BingeBuddy.Models"
             x:Class="BingeBuddy.Views.MyShowsPage"
             x:DataType="viewmodels:MyShowsViewModel"
             BackgroundColor="#0F0F0F"
             Title="My Shows">

    <Grid RowDefinitions="Auto,*" Padding="0">

        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" Padding="20,10" BackgroundColor="#1A1A1A">
            <Label Text="My Shows" 
                   FontSize="28" 
                   FontAttributes="Bold" 
                   TextColor="White" 
                   Margin="0,10,0,5"/>
            <Label Text="{Binding ShowCount, StringFormat='{0} shows tracked'}" 
                   FontSize="14" 
                   TextColor="#888888"/>
        </VerticalStackLayout>

        <!-- Content -->
        <Grid Grid.Row="1">
            <!-- Empty State -->
            <ScrollView IsVisible="{Binding HasShows, Converter={StaticResource InvertedBoolConverter}}">
                <VerticalStackLayout Padding="20" Spacing="20" VerticalOptions="Center">
                    <Label Text="📺" 
                           FontSize="60" 
                           HorizontalOptions="Center"/>
                    <Label Text="No Shows Yet" 
                           FontSize="24" 
                           FontAttributes="Bold" 
                           TextColor="White" 
                           HorizontalOptions="Center"/>
                    <Label Text="Search and add your first show to start tracking!" 
                           FontSize="16" 
                           TextColor="#888888" 
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>
            </ScrollView>

            <!-- Shows Grid -->
            <ScrollView IsVisible="{Binding HasShows}">
                <CollectionView ItemsSource="{Binding MyShows}"
                              Margin="15,15,15,0">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" 
                                       Span="2" 
                                       HorizontalItemSpacing="10" 
                                       VerticalItemSpacing="15"/>
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:UserShow">
                            <VerticalStackLayout Spacing="8">
                                <!-- Poster Image -->
                                <Frame Padding="0" 
                                       BackgroundColor="#2A2A2A" 
                                       CornerRadius="8"
                                       HasShadow="False">
                                    <Image Source="{Binding PosterUrl}"
                                           Aspect="AspectFill"
                                           HeightRequest="240"
                                           WidthRequest="160"/>
                                </Frame>

                                <!-- Show Title -->
                                <Label Text="{Binding Title}" 
                                       FontSize="14" 
                                       FontAttributes="Bold" 
                                       TextColor="White"
                                       MaxLines="2"
                                       LineBreakMode="TailTruncation"/>

                                <!-- Status -->
                                <Label Text="{Binding Status}" 
                                       FontSize="12" 
                                       TextColor="#888888"/>
                            </VerticalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </ScrollView>
        </Grid>

    </Grid>
</ContentPage>